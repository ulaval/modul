// this file is used by the orta.vscode-jest VSCODE extension to run and debug unit test from the modul-components package.

var config = require("./packages/modul-components/jest.config");

config.rootDir = './packages/modul-components/';

module.exports = config;
